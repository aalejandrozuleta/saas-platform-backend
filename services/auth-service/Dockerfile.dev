FROM node:24-alpine

WORKDIR /app

ENV PNPM_STORE_PATH=/pnpm-store

COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./

RUN corepack enable && corepack prepare --activate

COPY .npmrc ./
COPY tsconfig.base.json ./

COPY services ./services
COPY shared ./shared

# Instalar dependencias y generar cliente Prisma en una sola capa
# NOSONAR: pnpm install scripts required for prisma
RUN pnpm install --frozen-lockfile && \
    pnpm --filter auth-service exec prisma generate

WORKDIR /app/services/auth-service

EXPOSE 3001

ENTRYPOINT ["/bin/sh", "/app/services/auth-service/docker-entrypoint.sh"]
