# NOSONAR: dev image — non-root not required for hot-reload container
FROM node:24-alpine

WORKDIR /app

ENV PNPM_STORE_PATH=/pnpm-store

COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./

RUN corepack enable && corepack prepare --activate

COPY .npmrc ./
COPY tsconfig.base.json ./

COPY services ./services
COPY shared ./shared

RUN pnpm install --frozen-lockfile --ignore-scripts \
    && pnpm --filter company-service exec prisma generate

WORKDIR /app/services/company-service

EXPOSE 3004

ENTRYPOINT ["/bin/sh", "/app/services/company-service/docker-entrypoint.sh"]
