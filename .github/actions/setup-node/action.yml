name: Setup Node Environment
description: Setup Node, pnpm, cache and prisma

runs:
  using: composite
  steps:
    - name: Setup pnpm
      uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
      with:
        run_install: false

    - name: Setup Node
      uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
      with:
        node-version: 24
        check-latest: true
        cache: pnpm
        cache-dependency-path: pnpm-lock.yaml

    - name: Install dependencies
      shell: bash
      run: pnpm install --frozen-lockfile

    - name: Cache Prisma
      id: cache-prisma
      uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        path: |
          services/auth-service/node_modules/.prisma
          services/config-service/node_modules/.prisma
        key: prisma-${{ runner.os }}-${{ hashFiles('**/schema.prisma') }}-${{ hashFiles('pnpm-lock.yaml') }}

    - name: Generate Prisma Client
      if: steps.cache-prisma.outputs.cache-hit != 'true'
      shell: bash
      run: pnpm --filter auth-service exec prisma generate && pnpm --filter config-service exec prisma generate
