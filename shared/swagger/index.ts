/**
 * @remarks
 * Agrupa la configuración base de Swagger (`createSwaggerConfig`) y la
 * función de setup que la aplica a una app NestJS (`setupSwagger`).
 */
export * from './swagger.config';
export * from './swagger.setup';
