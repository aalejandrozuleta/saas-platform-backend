/**
 * @remarks
 * Agrupa el catálogo de permisos (`Permission`) y el mapa de permisos por
 * defecto para cada rol (`ROLE_PERMISSIONS`), usado como fuente de verdad
 * para el seed de la base de datos.
 */
export * from './permission.enum';
export * from './role-permissions.map';
