# This directory is NOT a Docker build context.
# All Dockerfiles in services/ use context: .. (repo root).
# The active .dockerignore is at the repo root: /.dockerignore
#
# This file exists only to prevent accidental Docker builds from within
# the docker/ directory. If you ever add a Dockerfile with context: ./docker,
# populate this file accordingly.
